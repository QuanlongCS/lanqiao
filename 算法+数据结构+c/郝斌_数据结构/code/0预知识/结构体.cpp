#include <stdio.h>
struct Stu{
	int id;
	char name[100];
	int age;
}; 
int main(){
//为什么要用结构体？
//类，弥补没有类的缺陷
	struct Stu student;
	student.age=
	printf("%d %s %d",student.id,student.name,student.age);
	 

	return 0;
}

